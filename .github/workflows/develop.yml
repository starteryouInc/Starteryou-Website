name: Deploy to AWS EC2

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Set up environment variables for CI
        run: |
          echo "MONGO_USER=${{ secrets.MONGO_USER }}" >> $GITHUB_ENV
          echo "MONGO_PASSWORD=${{ secrets.MONGO_PASSWORD }}" >> $GITHUB_ENV
          echo "MONGO_HOST=${{ secrets.MONGO_HOST }}" >> $GITHUB_ENV
          echo "MONGO_PORT=${{ secrets.MONGO_PORT }}" >> $GITHUB_ENV
          echo "MONGO_DB=${{ secrets.MONGO_DB }}" >> $GITHUB_ENV

      - name: Add SSL Certificates via Secrets
        run: |
          mkdir -p ./mongodb-certs
          echo "${{ secrets.MONGO_TLS_CA }}" > ./mongodb-certs/ca.crt
          echo "${{ secrets.MONGO_TLS_CERT }}" > ./mongodb-certs/server.pem
        shell: bash

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e
            # Ensure that the repository is cloned properly
            # Navigate to project directory or clone if it doesn't exist
            if [ ! -d "/home/ec2-user/Starteryou-Website/Starteryou" ]; then
              echo "Repository not found, cloning the repository..."
              mkdir -p /home/ec2-user/Starteryou-Website
              cd /home/ec2-user/Starteryou-Website
              git clone https://github.com/starteryouInc/Starteryou-Website.git -b develop Starteryou
            else
              echo "Repository found, pulling latest changes..."
              cd /home/ec2-user/Starteryou-Website/Starteryou
              git pull origin develop --ff-only || git pull origin develop --rebase
            fi

            # Verify repository contents
            echo "Listing the contents of the directory:"
            ls -al /home/ec2-user/Starteryou-Website/Starteryou

            # Ensure the user has write permissions to the directory
            sudo chown -R ec2-user:ec2-user /home/ec2-user/Starteryou-Website/Starteryou

            # Create an .env file dynamically on the server
            echo "Creating .env file..."
            cat <<EOT > /home/ec2-user/Starteryou-Website/Starteryou/.env
            NODE_ENV=production
            MONGO_USER=${{ secrets.MONGO_USER }}
            MONGO_PASSWORD=${{ secrets.MONGO_PASSWORD }}
            MONGO_HOST=${{ secrets.MONGO_HOST }}
            MONGO_PORT=${{ secrets.MONGO_PORT }}
            MONGO_DB=${{ secrets.MONGO_DB }}
            MONGO_TLS_CA=${{ secrets.MONGO_TLS_CA }}
            MONGO_TLS_CERT=${{ secrets.MONGO_TLS_CERT }}
            EOT

            # Check if the .env file is created correctly
            cat /home/ec2-user/Starteryou-Website/Starteryou/.env

            # Ensure Docker is running
            sudo systemctl start docker || sudo systemctl restart docker

            # Clean up old containers and rebuild
            docker-compose down --volumes --remove-orphans
            docker system prune -af --volumes

            # Start the application with the updated configuration
            docker-compose up -d --build
            docker ps
            docker logs starteryou-backend
            docker logs starteryou-frontend
